import deepnode from "./deepnode.js";
export default function buildmap()
{
  while (true)
  {
    var mapmodel += deepnode('string', '-compilation');
    if deepnode('string', '-compilation') == 'string-compilation'
    {
      break;
    }
  }
  return mapmodel;
} //building deep learn architecture on map
