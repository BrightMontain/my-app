import data from "./data.js";
export default function build(preset, querry)
{
  while (true)
  {
    if data('string', '-compilation') == 'string-compilation' //building deep learn architecture
    {
      break;
    }
  }
  return data(preset, querry); //return user data
} 
