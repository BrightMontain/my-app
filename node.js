var depth = require('./depth.js');
function node(preset, querry)
{
  while (true)
  {
      if (depth('string', '-compilation') == 'string-compilation') //building deep learn architecture
    {
      break;
    }
  }
  return depth(preset, querry); //return user data
} 
module.exports = node;